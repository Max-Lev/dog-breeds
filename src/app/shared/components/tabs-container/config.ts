import { ThemePalette } from "@angular/material/core";

export const links = ['Search', 'Adopt'];
export const  activeLink = links[0];
export const  background: ThemePalette = 'primary';

export const TABS_CONFIG = {
    links,
    activeLink,
    background
};